<html><head><base href="https://clearufo.space/">
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Video Forensics Services</title>
<style>
  :root {
    --primary-color: #121212;
    --secondary-color: #1e1e1e;
    --accent-color: #ffffff;
    --text-color: #cccccc;
  }
  
  body, html {
    margin: 0;
    padding: 0;
    font-family: 'Arial', sans-serif;
    background-color: var(--primary-color);
    color: var(--text-color);
  }
  
  .container {
    width: 100%;
    max-width: 1400px;
    margin: 0 auto;
  }
  
  header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 40px;
    position: relative;
  }
  
  .logo {
    display: flex;
    align-items: center;
    font-size: 24px;
    font-weight: bold;
    color: var(--accent-color);
  }
  
  .logo img {
    width: 50px;
    height: 50px;
    margin-right: 10px;
  }
  
  nav ul {
    list-style-type: none;
    display: flex;
  }
  
  nav ul li {
    margin-left: 20px;
  }
  
  nav ul li a {
    color: var(--text-color);
    text-decoration: none;
    transition: color 0.3s ease;
  }
  
  nav ul li a:hover {
    color: var(--accent-color);
  }
  
  .hero {
    text-align: left;
    padding: 100px 40px;
    position: relative;
  }
  
  .hero h1 {
    font-size: 48px;
    color: var(--accent-color);
    margin-bottom: 20px;
  }
  
  .hero p {
    font-size: 18px;
    max-width: 600px;
  }
  
  .cta-button {
    display: inline-block;
    padding: 12px 24px;
    background-color: var(--accent-color);
    color: var(--primary-color);
    text-decoration: none;
    border-radius: 5px;
    font-weight: bold;
    margin-top: 20px;
    transition: background-color 0.3s ease;
  }
  
  .cta-button:hover {
    background-color: #e0e0e0;
  }
  
  .section {
    padding: 50px 40px;
  }
  
  .section h2 {
    color: var(--accent-color);
    font-size: 36px;
    margin-bottom: 30px;
  }
  
  .card-container {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
  }
  
  .card {
    background-color: var(--secondary-color);
    border-radius: 10px;
    padding: 20px;
    margin-bottom: 20px;
    width: calc(50% - 20px);
  }
  
  .card h3 {
    color: var(--accent-color);
    font-size: 24px;
    margin-bottom: 10px;
  }
  
  .card p {
    font-size: 16px;
  }
  
  .video-player {
    background-color: var(--secondary-color);
    border-radius: 10px;
    padding: 20px;
    margin-top: 30px;
  }
  
  footer {
    text-align: center;
    padding: 20px 40px;
    background-color: var(--secondary-color);
  }
  
  .curved-element {
    position: absolute;
    top: 0;
    right: 0;
    width: 300px;
    height: 300px;
    background-color: var(--accent-color);
    opacity: 0.05;
    border-bottom-left-radius: 100%;
  }
  
  .circle-element {
    position: absolute;
    top: 50px;
    right: 50px;
    width: 100px;
    height: 100px;
    border: 2px solid var(--accent-color);
    border-radius: 50%;
    opacity: 0.1;
  }
  
  .features {
    display: flex;
    justify-content: space-between;
    margin-top: 50px;
  }
  
  .feature {
    background-color: var(--secondary-color);
    border-radius: 10px;
    padding: 20px;
    width: calc(33% - 20px);
  }
</style>
</head>
<body>
  <div class="curved-element"></div>
  <div class="circle-element"></div>
  
  <div class="container">
    <header>
      <div class="logo">
        <img src="/a/569c309f-ad33-4a0a-a9e2-9e8a639dcc9e" alt="Clear UFO Space logo" width="50" height="50">
        <span>Video Forensics</span>
      </div>
      <nav>
        <ul>
          <li><a href="#home">Home</a></li>
          <li><a href="#services">Services</a></li>
          <li><a href="#about">About</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </nav>
    </header>
    
    <section class="hero" id="home">
      <h1>Video Forensics Services</h1>
      <p>Uncover the truth with our cutting-edge video analysis techniques</p>
      <a href="#contact" class="cta-button">Learn More</a>
    </section>
    
    <section class="section" id="services">
      <h2>Our Services</h2>
      <div class="card-container">
        <div class="card">
          <h3>Video Enhancement</h3>
          <p>Improve clarity and visibility of crucial details in your video evidence.</p>
        </div>
        <div class="card">
          <h3>Authentication</h3>
          <p>Verify the integrity and authenticity of your video footage.</p>
        </div>
      </div>
    </section>
    
    <section class="section" id="about">
      <h2>Video Analysis</h2>
      <div class="video-player">
        <div style="background: #2a2a2a; height: 300px; display: flex; align-items: center; justify-content: center; color: #fff; border-radius: 10px;">
          Video Player Placeholder
        </div>
      </div>
    </section>
    
    <section class="section" id="features">
      <h2>Featured Services</h2>
      <div class="features">
        <div class="feature">
          <h3>Timestamp Analysis</h3>
          <p>Determine precise timing of events in your video evidence.</p>
        </div>
        <div class="feature">
          <h3>Facial Recognition</h3>
          <p>Identify individuals in video footage using advanced AI algorithms.</p>
        </div>
        <div class="feature">
          <h3>Object Detection</h3>
          <p>Locate and track specific objects or elements within video sequences.</p>
        </div>
      </div>
    </section>
  </div>
  
  <footer>
    <p>&copy; 2023 Video Forensics Services. All rights reserved.</p>
  </footer>

<script>
document.addEventListener('DOMContentLoaded', (event) => {
    const navLinks = document.querySelectorAll('nav ul li a');
    const sections = document.querySelectorAll('.section');
    
    window.addEventListener('scroll', () => {
        let current = '';
        sections.forEach(section => {
            const sectionTop = section.offsetTop;
            const sectionHeight = section.clientHeight;
            if (pageYOffset >= sectionTop - sectionHeight / 3) {
                current = section.getAttribute('id');
            }
        });

        navLinks.forEach(link => {
            link.classList.remove('active');
            if (link.getAttribute('href').slice(1) === current) {
                link.classList.add('active');
            }
        });
    });

    const cards = document.querySelectorAll('.card, .feature');
    cards.forEach(card => {
        card.addEventListener('mouseenter', () => {
            card.style.transform = 'scale(1.05)';
            card.style.transition = 'transform 0.3s ease';
        });
        card.addEventListener('mouseleave', () => {
            card.style.transform = 'scale(1)';
        });
    });
});
</script>

</body></html>
